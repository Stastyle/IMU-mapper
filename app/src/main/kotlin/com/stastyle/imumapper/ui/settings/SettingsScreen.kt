package com.stastyle.imumapper.ui.settings

import androidx.compose.runtime.Composable
import com.stastyle.imumapper.ui.common.PlaceholderScreen

/** Carry position, units, check for updates, about. */
@Composable
fun SettingsScreen(onBack: () -> Unit) {
    PlaceholderScreen(title = "Settings", onBack = onBack)
}

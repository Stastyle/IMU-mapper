# tools

Offline helpers for algorithm work. `replay.py` reads a raw `.imul` log exported from the app,
runs the same processing steps as the Kotlin pipeline, and plots the path and intermediate
signals. Implemented by the tools work item after the PDR pipeline exists.
